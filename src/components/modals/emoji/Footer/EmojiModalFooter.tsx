import BlackButton from '../../../buttons/BlackButton';

const EmojiModalFooter = () => {
  return (
    <div className="flex justify-end">
      <BlackButton onClick={() => {}} text="Selecionar" />
    </div>
  );
};

export default EmojiModalFooter;
