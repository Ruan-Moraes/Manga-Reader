# Auth Design Reference

This folder contains the original static/auth prototype assets used as visual reference for the mobile authentication screens.

It is intentionally outside `app/` and `src/` so it cannot be imported by runtime code.
