# URL base da API — em dev, deixe vazio para usar o Vite proxy.
# Em produção, aponte para o api real (ex.: https://api.toonlira.com).
VITE_API_BASE_URL=

# Path base de roteamento/deploy. GitHub Pages usa "/Toonlira".
# Em outro ambiente use "/" ou o sub-path desejado.
VITE_BASE_URL=/Toonlira
